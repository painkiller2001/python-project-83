[project]
name = "hexlet-code"
version = "0.1.0"
description = "Page Analyzer"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
	"flask",
	"gunicorn>=26.0.0",
	"python-dotenv>=1.2.2",
	"ruff",
]
